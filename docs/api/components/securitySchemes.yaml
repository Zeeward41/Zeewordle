# Session + cookie
SessionAuth:
  type: apiKey
  in: cookie
  name: connect.sid  # 💡 Remplace par le vrai nom de ton cookie de session
  description: |
    Express session cookie.
    Must be sent automatically by browser.
